EventBuilder unit tests completed successfully

- Created EventBuilderTest.kt with all required test cases
- Tests cover buildScreenView with correct event creation and timestamp
- Tests cover buildUIInteraction with and without screen context  
- Tests cover custom UIAction scenarios
- All tests pass with successful build
- Used JUnit 4 with proper imports and Given-When-Then naming convention
